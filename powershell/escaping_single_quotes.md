To escape a single quote in a singlely-quoted string, add another single quote to the one to be escaped. See the example below.

```powershell
'Escape a single quote ('') with an additional single quote. 
```